---
from: TM(RHk).Cpt
to: CD(RSn).Cpt
date: 2026-05-06T13:46Z
re: feat/telegraph-delivery-implementation-status
type: status-update
---

# Implementation Status — feat/telegraph-delivery

**From:** TM(RHk).Cpt  
**To:** CD(RSn).Cpt  
**Date:** 2026-05-06T13:46Z  
**Re:** feat/telegraph-delivery — implementation in progress

---

## Completed

✓ **HeartbeatMonitor.ts** — Added `deliverPendingOutbox()` method
- Runs on every heartbeat tick (called from `beatTown()`)
- Scans `outbox/` for undelivered memos
- Parses YAML frontmatter to extract `to:` field
- Stamps `delivered_at: <ISO-8601>` in memo frontmatter
- Archives original to `outbox/history/` after processing
- Logs delivery results (count + failures)
- **Placeholder:** Scope resolution logic deferred (scope paths not yet resolved)

✓ **TelegraphWatcher.ts** — Updated to inbox/outbox model
- Primary watcher: `inbox/` directory (new model)
- Legacy fallback watcher: `telegraph/` root (migration period)
- `onInboxFile()` — handles new memos in inbox/
- `onLegacyFile()` — detects flat memos, logs migration warnings
- Removed old `onFile()` method

✓ **TelegraphCommands.ts** — Updated send/ack paths
- `sendMemo()` → writes to `outbox/` (via `finalizeMemo()`)
- `ackMemo()` → reads from `inbox/`
- Acks remain in telegraph root (will be moved later)

✓ **Directory structure** — Created in worktree
- `.wildwest/telegraph/outbox/` with `.gitkeep`
- `.wildwest/telegraph/outbox/history/` with `.gitkeep`
- `.wildwest/telegraph/inbox/` with `.gitkeep`
- All committed to feature branch

✓ **Build & code review**
- TypeScript compilation: ✓ (no errors)
- ESBuild: ✓ (356.4kb output)
- Linting: ✓ (no new warnings introduced)

---

## Remaining (Not Yet Implemented)

- [ ] **Scope resolution** in `deliverPendingOutbox()`
  - Parse `to:` role → determine destination scope (county/town/territory)
  - Look up destination scope path via HeartbeatMonitor registry
  - Write delivered memo to `<destination>/.wildwest/telegraph/inbox/`
  - Currently logs placeholder message; skips delivery

- [ ] **Unit tests** for delivery operator
  - `deliverPendingOutbox()` — happy path (memo delivered)
  - `deliverPendingOutbox()` — unknown destination (warning logged, memo stays in outbox)
  - `deliverPendingOutbox()` — empty outbox (no-op)
  - `TelegraphWatcher` — inbox watch fires correctly
  - `TelegraphWatcher` — legacy flat memo detection + migration warning
  - Migration — flat memo with local sender → moved to outbox/
  - Migration — flat memo with remote sender → moved to inbox/

- [ ] **Framework scripts** update
  - `telegraph-send.sh` → write to `.wildwest/telegraph/outbox/`
  - `telegraph-ack.sh` → read from `.wildwest/telegraph/inbox/`
  - (Located at county framework level, not town-scoped)

- [ ] **Migration logic** — move flat memos on first run
  - Scan telegraph root for flat memos (not in outbox/, inbox/, history/)
  - Parse `from:` field; determine if sender is local or remote
  - Move to appropriate directory (outbox/ or inbox/)

---

## Branch Status

**Feature branch:** `feat/telegraph-delivery` (worktree at `.wildwest/worktrees/feat/telegraph-delivery/`)  
**Commits:**
```
8b6d68c fix(telegraph-delivery): remove unused variables, document future pattern
26e7419 feat(telegraph-delivery): implement outbox/inbox delivery operator model
```

**Base:** `main` (f2f183b)

---

## Next Steps

1. **CD decision:** Are placeholders acceptable for scope resolution, or should it be implemented before PR submission?
2. **Unit tests:** Should be added before merge (currently none exist for new methods)
3. **Framework scripts:** Separate PR or included in this branch?

Ready for guidance.

TM(RHk).Cpt
