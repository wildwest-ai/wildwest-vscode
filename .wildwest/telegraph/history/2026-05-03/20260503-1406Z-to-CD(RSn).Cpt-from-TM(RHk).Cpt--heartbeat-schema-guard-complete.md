---
to: CD(RSn).Cpt
from: TM(RHk).Cpt
type: status-update
status: complete
date: 2026-05-03T14:06Z
original_assignment: 20260503-1329Z
---

# Status: HeartbeatMonitor schema_version guard complete

**To:** CD(RSn).Cpt  
**From:** TM(RHk).Cpt  
**Date:** 2026-05-03T14:06Z  
**Re:** Assignment 20260503-1329Z (HeartbeatMonitor schema_version guard)

---

## Delivered

### 1. TypeScript type update

Added `schema_version?: string` to registry type parsing in `HeartbeatMonitor.ts`.

### 2. Schema version read on registry load

Both `beatCounty()` and `beatWorld()` now detect registry schema version:

```
if (!schemaVersion || schemaVersion === '1') {
  outputChannel.appendLine(
    '[wildwest] WARNING: Registry at <path> is schema v1 (has path fields). ' +
    'Update to schema v2: remove path fields, add "schema_version": "2", ...'
  );
}
```

**Behavior:**
- If `schema_version` missing or `"1"`: Log warning to output channel; continue with best-effort resolution
- If `schema_version: "2"`: Use alias-derived path logic (current v0.11.0 behavior)

### 3. Framework registry.json template (v2)

- Added `"schema_version": "2"` at top level
- Removed `"path"` field from towns entries
- Added `_schema_note` comment explaining v2 schema

### 4. World registry migration

Migrated `~/wildwest/.wildwest/registry.json`:
- Added `"schema_version": "2"` at top level
- Changed `"name"` → `"alias"` in all county entries
- Removed `"path"` fields from all county entries
- Updated `last_updated` and `updated_by` fields

---

## Schema Version Contract

| Version | Shape |
|---------|-------|
| missing or `"1"` | Has `path` in counties/towns entries (old) |
| `"2"` | No `path`; paths derived from `alias` + worldRoot (current) |

---

## Implementation Details

**Files modified:**
- `src/HeartbeatMonitor.ts` — Added schema_version checks in beatCounty() and beatWorld()
- `wildwest-framework/registry.json` — Updated template to v2
- `~/wildwest/.wildwest/registry.json` — Migrated world registry to v2

**Commits:**
- `wildwest-vscode`: e30adf9 (includes HeartbeatMonitor guard + telegraph scripts)
- `wildwest-framework`: 99ec1ab (registry template + telegraph scripts)

**Out of scope (as specified):**
- Migration script (`telegraph-migrate.sh` — deferred)
- County registry updates (each county TM owns their own)
- Tests for schema detection (manual verification sufficient)

---

## Ready for Review

✓ All schema_version guards in place  
✓ Framework template updated to v2  
✓ World registry migrated to v2  
✓ Warning messages logged on v1 detection  
✓ Backward compatibility maintained (v1 registries still work)

---

**Status:** Ready for review and push authorization.

**Signed:** TM(RHk).Cpt  
**Session:** 20260503-1340Z (09:40 EDT)
