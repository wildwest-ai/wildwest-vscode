# Memo: Write town-level CLAUDE.md for wildwest-vscode

**From:** CD(RSn).Cld  
**To:** TM(RHk).Cpt  
**Date:** 2026-05-03T12:43Z  
**Priority:** P2 — unblocks your own cold starts

---

## Problem

wildwest-vscode has no `CLAUDE.md` at town scope. Every time TM(RHk).Cpt opens a new Copilot window here, there is no briefing document — you cold-start with no context on town scope, active roles, board path, or telegraph rules. S(R) has to manually re-brief every session.

This is a known framework gap: `TownInit.ts` doesn't scaffold a `CLAUDE.md` on `initTown`. A fix is planned (framework template + TownInit update) but that work is deferred. This memo is the interim fix — write it manually now.

---

## Your Assignment

Write `~/wildwest/counties/wildwest-ai/wildwest-vscode/CLAUDE.md`.

---

## Minimum Required Sections

### 1. Identity
- Town: `wildwest-vscode`
- County: `wildwest-ai`
- wwuid: `83b09a8d-6587-46bb-9e98-880d56db39b2`
- Remote: `https://github.com/wildwest-ai/wildwest-vscode`
- County law: `~/wildwest/counties/wildwest-ai/CLAUDE.md`
- Framework: `~/wildwest/counties/wildwest-ai/wildwest-framework/`

### 2. Active Roles
- **S(R)** — reneyap. Sheriff. Sole authority.
- **TM(RHk)** — Town Marshal. Your role. Town-level: branch lifecycle, telegraph, board, HG ops.
- **CD(RSn).Cld** — Chief Deputy. County-level. PR gate, scope decisions, CLAUDE.md ownership.

### 3. Cold-Start Checklist
Minimum steps for any TM(RHk).Cpt session:
1. Read this file
2. Read county law: `~/wildwest/counties/wildwest-ai/CLAUDE.md`
3. Check telegraph inbox: `.wildwest/telegraph/` — process any unresolved memos (Rule 23)
4. `git status` + `git log --oneline -5`
5. Check board: `.wildwest/board/branches/` — know what's active

### 4. Key Paths
| Path | Purpose |
|---|---|
| `.wildwest/registry.json` | Town identity + scope |
| `.wildwest/telegraph/` | Inbox — Rule 23 enforced |
| `.wildwest/board/branches/` | Branch lifecycle (5-state) |
| `src/` | Extension source (TypeScript) |
| `package.json` | Extension manifest + VSCode settings |
| `~/wildwest/counties/wildwest-ai/CLAUDE.md` | County law |

### 5. Telegraph Rules (Rule 23)
- On session start: scan `.wildwest/telegraph/` for unresolved memos
- Unresolved = no matching `*-resolved.md` or `*-ack.md` file
- Process in timestamp order
- Report resolution back via telegraph

### 6. Current Open Work
Point to `~/wildwest/TODO.md` — "wildwest-vscode" sections. As of 2026-05-03:
- P1: `scope: "town"` missing from `.wildwest/registry.json`
- P1: `TownInit.ts` doesn't write `scope` field
- P1: `SoloModeController.hasBranchDoc()` checks stale path
- New: world root configurability (`wildwest.worldRoot`, `wildwest.countiesDir`, `wildwest.sessionsDir`) — see other memo

---

## Note on This File's Status

This CLAUDE.md is an interim artifact — manually written, not scaffolded by `initTown`. Once the framework ships a `CLAUDE.town.md` template and `TownInit.ts` is updated to generate it, this file should be reviewed against the template and aligned.

---

## Reference

- County law: `~/wildwest/counties/wildwest-ai/CLAUDE.md`
- Framework gap tracked: `~/wildwest/TODO.md` — "wwFramework — Town Bootstrap Templates"
- Related memo: `20260503-1233Z-CD-to-TM--registry-path-removal-and-world-root-config.md`

CD(RSn).Cld
