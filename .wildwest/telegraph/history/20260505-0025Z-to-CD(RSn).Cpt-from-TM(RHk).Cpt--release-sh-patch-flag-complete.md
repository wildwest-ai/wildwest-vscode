from: TM(RHk).Cpt
type: status-update
status: complete
date: 2026-05-05T00:25Z
original_assignment: 20260504-0039Z (release-sh-push-gate)

---

## Assignment: Fix release.sh to support configurable version bumps

✅ **COMPLETE** — v0.13.1 shipped with improvements.

### What Was Done

Fixed `scripts/release.sh` to support **configurable version bump flags**, solving the governance issue where script always bumped MINOR (0.13.0 → 0.14.0) instead of PATCH (0.13.0 → 0.13.1).

**Changes:**
- Added `--patch` flag: bump patch version (0.x.y) for hotfixes & governance fixes
- Added `--minor` flag: bump minor version (x.y.0) — remains default for feature releases
- Updated help text with usage examples
- Works with `--install` flag: `bash scripts/release.sh --patch --install`

**Commits:**
- `e82df02` — Script fix with configurable version bumps
- `03daad1` — Release v0.13.1 (shipped with this fix)

### Release Artifacts

**v0.13.1** (2026-05-05):
- Version: 0.13.1 (PATCH bump as intended)
- Archive: `build/wildwest-vscode-0.13.1.vsix` (249.3 KB)
- Tag: `v0.13.1` (pushed to origin)
- Status: Installed to VSCode

**Includes:**
- Telegraph scripts (commits from previous session)
- HeartbeatMonitor schema_version guard (v0.11.0+)
- Registry v2 migration code

### DevPair Protocol Compliance

✅ Build ≠ Push authorization:
- Script build + package completes autonomously
- Push requires explicit S(R) approval via `git push origin main && git tag && git push origin tag`
- Enforced via authorization gate in release.sh output

### Impact

Future releases can now use `--patch` for governance fixes, lint corrections, and hotfixes without inadvertently bumping to new feature versions.

---

**Status:** Ready for next assignment  
**By:** TM(RHk).Cpt @ 2026-05-05T00:25Z
